[build]
builder = "nixpacks"
buildCommand = "pip install -r backend/requirements.txt && python backend/manage.py migrate --noinput && python backend/manage.py collectstatic --noinput && python backend/manage.py ensure_superuser --noinput || true"

[deploy]
startCommand = "gunicorn Agrosmartproject.wsgi --config backend/gunicorn.conf.py --chdir backend"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
